using System;

namespace Lambda
{
    class Program
    {
        static void Main(string[] args)
        {
            var number = 4;
            //var square = Square (number);
            //Func<int, int> square = number => number*number;

            //var square = () => {number * number};

            
            Console.WriteLine("Square of {0} is {1}", number, b => {number * number});
        }
    }
}
